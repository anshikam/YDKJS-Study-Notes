a = b * 2;
a,b are Variables
2 is a literal value
= and * are operators

Program execution in JavaScript
It's typically asserted that Javascript is interpreted, because your JavaScript code is processed each time it's run.
But that's not correct.
The JavaScript engine actually compiles the program on the fly and then immediately runs the compiled code.